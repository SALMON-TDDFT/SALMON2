# Time-dependent ELF diagnostic

Goal: measure instantaneous current-corrected ELF every10 steps on the existing Si snapshots, before considering any ELF-to-alpha rule.
Use Burnus/Marques/Gross physics/0404126 equations1–4 with one-spin density, tau=sum wk| (grad+ik)u |² (no factor1/2), paramagnetic orbital current and consistent density gradient. The vector potential cancels in tau-j²/n if introduced consistently. Do not insert macroscopic pseudopotential-corrected current into this local orbital expression.

Implement NumPy spectral derivatives using existing checkpoint/Fourier infrastructure. Test single orbital, occupied-unitary invariance, constant boost invariance and finite Fermi gas reference. Compute fields and electron-weighted distribution at every10 steps, bond-centered and interstitial diagnostics; retain maps at selected times. Report ELF0.5 as a cold uniform-gas reference, not a sufficient test for metallic transport or dielectric screening. No new TDDFT runs, k scan or alpha feedback.
