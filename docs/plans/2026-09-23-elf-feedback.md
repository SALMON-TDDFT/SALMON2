# ELF-dependent alpha feedback

User requests alpha0 initially and zero at the uniform-gas ELF limit, with time dependence. Retain previous no-upper-clip and every10-step requirements.
Model: Q=<n(ELF-.5)^2>/<n>; alpha=alpha0 Q/Q0. No cancellation between ELF above/below .5. This is an empirical instantaneous localization model, not a dielectric derivation; hot uniform distributions need not have ELF=.5.
Implementation: tdcdft_screening='elf', tdcdft_elf_stride=10. Compute one-spin current-corrected ELF using SALMON's native symmetric finite-difference gradient plus ik, summed occupation/k-weighted moments, MPI reductions. Normalize to the actual initial Q computed with the same derivative. Hold alpha between measurements; continue polarization and XC evolution each step using the polarization-consistent closure. No current/field ratio or laser gate. Optional existing stop-time freezes the measure.
Store Q0 and stride in checkpoint v4, retain backward compatibility for old modes and reject ELF restarts without its state. Report Q and Q0 with alpha in rt_xc. Test endpoints, no upper clipping, gauge/current cancellation, cadence, restart at a non-update step, input guards, MPI consistency, native-versus-offline ELF and existing regressions. Compare same4³ Si none/weak/strong and fixed-alpha0.2 controls, impulse response and short strong trajectory. No k scan or push.
