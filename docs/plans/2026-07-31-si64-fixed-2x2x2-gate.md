# Si64 Fixed 2x2x2 Gate Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Accept the fixed `2x2x2`, eight-MPI, buffer-5 Si64 production profile while retaining buffer 6 as required diagnostic evidence.

**Architecture:** Keep the four-row materialization and individual validation code, but apply the `1e-4` candidate-window convergence gate only within the fixed buffer-5 production profile. Preserve buffer-6 rows as mandatory diagnostics and continue requiring the normal reference, checkpoint reuse, and forbidden-route absence.

**Tech Stack:** Python 3, Fortran 2008 production executable, MPI, clean parent-prerequisite overlay.

---

### Task 1: Make the fixed matrix contract RED

**Files:**

- Modify: `tests/dg/check_si64_overlapping_wannier_gate.py`
- Modify: `tests/dg/run_si64_overlapping_wannier_gate.py`

**Steps:**

1. Add a source-level assertion or self-test requiring exactly one accepted
   decomposition, `2x2x2`.
2. Require both buffer profiles and both candidate windows.
3. Require eight MPI ranks and one OpenMP thread for every row.
4. Run the checker-focused test and observe failure while `4x2x1` remains
   in the Cartesian product.

### Task 2: Restrict runner and checker

**Files:**

- Modify: `tests/dg/check_si64_overlapping_wannier_gate.py`
- Modify: `tests/dg/run_si64_overlapping_wannier_gate.py`

**Steps:**

1. Replace the decomposition collection with the single `2x2x2` entry.
2. Simplify buffer profiles to `(5,5,5)` and `(6,6,6)`.
3. Remove the equivalent-physical-decomposition comparison loop.
4. Apply candidate-window convergence only to buffer 5; validate buffer 6
   individually without a cross-buffer acceptance threshold.
5. Run Python syntax and source-contract verification and observe PASS.

### Task 3: Update evidence semantics

**Files:**

- Modify: `docs/plans/2026-07-27-si64-overlapping-wannier-results.md`

**Steps:**

1. Record why `4x2x1` is excluded for the 32-cubed Si64 system.
2. Preserve its failed logs as diagnostic evidence.
3. State that MPI-count invariance and larger-system decomposition
   invariance are deferred.
4. State explicitly that Task 10 remains blocked until the revised gate
   passes.
5. Run `git diff --check`.

### Task 4: Run the revised gate

**Files:**

- Evidence: `/Users/otobetoshihito/SALMON-dev/SALMON2_RTDG-task9-results/`

**Steps:**

1. Re-run all focused route, projection, construction, solver/density,
   SCF, checkpoint, and row-storage verification on 1, 2, 4, and 8 ranks
   where applicable.
2. Rebuild the clean parent-prerequisite overlay.
3. Run a fresh four-row fixed `2x2x2` matrix on eight MPI ranks and one
   OpenMP thread.
4. Run the public checker and require PASS.
5. Perform specification and code-quality review and resolve all
   Critical/Important findings.
6. Proceed to Task 10 only if every revised Task 9 gate passes.

No commit is made while Task 9 remains unaccepted.
